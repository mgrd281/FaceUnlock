{
  "version": "1.0.1",
  "notes": "Lock-screen face unlock now requires a single blink, which a photograph cannot produce. Fixes a broker code-signing pin that prevented the lock-screen path from ever completing, and a Keychain fallback bug that silently destroyed the enrolled profile.",
  "download": "https://github.com/mgrd281/FaceUnlock/releases/latest"
}
